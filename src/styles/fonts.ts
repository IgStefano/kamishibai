import { Dosis, Noto_Sans } from "@next/font/google";

export const dosis = Dosis({ subsets: ["latin"], weight: "500" });
export const notoSans = Noto_Sans({
  subsets: ["latin"],
  weight: "500",
});
